import React from "react";
import SnackbarView from "./SnackbarView";

const SnackbarContainer = (props: any) => {
  const obj = {};
  return <SnackbarView {...obj} />;
};

export default SnackbarContainer;
